{
  "name": "corrupted-exe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CORRUPTED.EXE — a deceptive trick platformer (Phaser 3, CrazyGames/Poki)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:cg": "vite build --mode crazygames",
    "build:poki": "vite build --mode poki",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^5.4.10"
  }
}
