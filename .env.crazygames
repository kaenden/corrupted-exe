# Loaded by `npm run build:cg` (vite build --mode crazygames).
# Selects the CrazyGames SDK v3 ad provider at build time. Plain `npm run build`
# (GitHub Pages / itch) leaves this unset → ad-free NullProvider, no SDK loaded.
VITE_AD_PROVIDER=crazygames
