# Loaded by `npm run build:playgama` (vite build --mode playgama).
# Selects the Playgama Bridge provider at build time (static bridge.playgama.com script in <head>).
# Plain `npm run build` leaves this unset → ad-free NullProvider, no portal SDK.
VITE_AD_PROVIDER=playgama
